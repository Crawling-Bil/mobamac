<?xml version='1.0' encoding='utf-8'?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>MobaMac</title>
    <description>Updates for MobaMac</description>
    <language>en</language>
    <item>
      <description>&lt;ul&gt;&lt;li&gt;MobaMac updates itself. It checks once a day, shows what changed, and installs the new version in place. No more downloading a zip, deleting the old app and dragging in the new one. MobaMac &amp;gt; Check for Updates checks right away, and the item below it turns the daily check off.&lt;/li&gt;&lt;li&gt;The app is now signed with a stable self-signed certificate instead of a throwaway ad-hoc one. Without that, macOS treats each build as a different app and asks for Keychain access to every saved SSH password again after every update. Building from source now needs a certificate created once; the README has the four steps.&lt;/li&gt;&lt;li&gt;Session logs are plain text. Color codes, cursor movement and the redrawing a prompt does are stripped, so a log reads the way the screen did and can go straight into a report. This covers SSH, SSH-1, Telnet, Serial and the local terminal alike. A progress bar or spinner is kept as the line it finished on, not every frame it drew.&lt;/li&gt;&lt;li&gt;The status bar no longer covers the last row of the terminal. It also made the device believe the screen was a row taller than it was, so long output like &amp;quot;show running-config&amp;quot; paged against the wrong height.&lt;/li&gt;&lt;li&gt;There is a Settings window (Cmd-,). It holds the appearance setting, the update options, and everything about logging.&lt;/li&gt;&lt;li&gt;The app follows System, Light or Dark, whichever you pick. This is the app&amp;#x27;s own windows only: the terminal keeps its own color theme, so a dark terminal in a light app works the way it always did.&lt;/li&gt;&lt;li&gt;Session logs can go in a folder of your choosing instead of ~/Library/Logs/MobaMac. New sessions use the new folder; sessions already open keep writing where they started. If the folder turns out to be unavailable, on an unplugged drive say, the session still connects and its log goes to the default folder, with a note in the status bar.&lt;/li&gt;&lt;li&gt;Old logs are deleted only from the folder logging currently points at, and only MobaMac&amp;#x27;s own .log and .raw files, since the folder you pick may well have your own files in it.&lt;/li&gt;&lt;li&gt;Closing a tab whose session is still connected now asks first, however you close it. Tabs that already dropped, failed or ended close straight away, and so does a local terminal, because nothing is lost there. The dialog has &amp;quot;Don&amp;#x27;t ask again&amp;quot;, and Settings has the switch to turn it back on.&lt;/li&gt;&lt;li&gt;Quitting, or closing the window, with sessions still connected asks once for all of them rather than once per tab.&lt;/li&gt;&lt;li&gt;Tabs can be switched from the keyboard: Cmd-1 to Cmd-8 for a tab by position, Cmd-9 for the last one, Cmd-Shift-[ and Cmd-Shift-] to move left and right, and Ctrl-Tab and Ctrl-Shift-Tab for the same. The Window menu lists the open tabs with their keys. Macros keep Opt-Cmd and did not change.&lt;/li&gt;&lt;li&gt;Two optional habits from MobaXterm and PuTTY, both off by default and both in Settings under Terminal: &amp;quot;Copy on select&amp;quot; and &amp;quot;Right-click pastes&amp;quot;. With right-click paste on, Control-click still opens the context menu, and a multi-line paste still asks first.&lt;/li&gt;&lt;/ul&gt;</description>
      <title>1.16</title>
      <pubDate>Wed, 23 Sep 2026 18:49:42 +0700</pubDate>
      <link>https://github.com/Crawling-Bil/mobamac/releases/tag/v1.16.0</link>
      <sparkle:version>22</sparkle:version>
      <sparkle:shortVersionString>1.16</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
      <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
      <enclosure url="https://github.com/Crawling-Bil/mobamac/releases/download/v1.16.0/MobaMac-1.16.zip" length="6871716" type="application/octet-stream" sparkle:edSignature="h88yGXnjwPmXPKK0bI4ksfSduFyB7k+ovft7g2ariOLVprQEp+R9Qo55UImCI85w7i7iV5W7b3g/YHGDLX/WDQ==" />
    </item>
  </channel>
</rss>